import 'package:flutter/material.dart';

class ManageListing extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("manage listing page")),
    );
  }
}